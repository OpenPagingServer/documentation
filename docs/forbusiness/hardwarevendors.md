# Hardware Vendors

If your a hardware vendor, you can use the SDK to build a module. Open Paging Server has the capability to support both packaged open-source and compiled closed-source endpoint modules. 

We are also open to working with hardware vendors to help support the process of building modules. Contact contact@openpagingserver.org.